import { createContext } from "react"

export const LightContext = createContext(null)

export const ModelContext = createContext(null)
export const UpdateModelContext = createContext(null)

export const LoginContext = createContext(null)
export const UpdateLoginContext = createContext(null)

